/* Games that are planned but not built. They live here rather than in engine.js
   so the engine still knows nothing about any specific game, and they appear on
   the launcher as honest dimmed tiles instead of the grid pretending to be a
   one-game shelf. Delete an entry the moment its real module registers. */
